/*
 * Placeholder: upstream `perftest` provides ib_send_lat, ib_write_bw, etc.
 * Install on Ubuntu: `apt install perftest` (package name may vary).
 */
#include <stdio.h>
int main(void) {
  puts("Use packaged perftest binaries instead of vendoring sources here.");
  return 0;
}
